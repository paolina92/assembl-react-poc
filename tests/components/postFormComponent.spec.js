import React, {Component} from 'react';
import renderer from 'react-test-renderer';
import PostForm from '../../src/js/components/postFormComponent';

describe('Test if component is rendered', () => {
  it('Should render a post form component', () => {
    // const component = renderer.create(<PostForm discussionId='29'/>);
    // let tree = component.toJSON();
    // expect(tree).toMatchSnapshot();
  });
});